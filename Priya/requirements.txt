# Bias evaluation pipeline — dependencies aligned with src/ + notebook
# Install: pip install -r requirements.txt

# Hugging Face: dataset loading (StereoSet, CrowS-Pairs, BBQ) + Inference API
datasets>=2.14.0
huggingface_hub>=0.20.0
requests>=2.28.0
httpx>=0.24.0
tqdm>=4.65.0

# Data / statistics
numpy>=1.21.0
pandas>=1.3.0
scipy>=1.7.0
pyarrow>=12.0.0

# Visualization (scripts/generate_charts.py + notebook)
matplotlib>=3.5.0
seaborn>=0.12.0

# --- Optional: local model inference / tokenizer tooling (not required for API-only runs)
# torch>=2.0.0
# transformers>=4.35.0
